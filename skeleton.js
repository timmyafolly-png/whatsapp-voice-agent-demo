const stage=document.querySelector('#stage'),play=document.querySelector('#play'),replay=document.querySelector('#replay'),progress=document.querySelector('#progress'),phase=document.querySelector('#phase');let paused=false,start=performance.now(),offset=0;function setMotion(state){document.querySelectorAll('.skeleton-wrap,.lumbar-hotspot,.zoom-card').forEach(el=>el.style.animationPlayState=state)}function tick(now){if(!paused){const t=((now-start+offset)%10000)/10000;progress.style.width=(t*100)+'%';phase.textContent=t<.56?'ROTATING · 01 / 03':'LUMBAR ZOOM · 02 / 03'}requestAnimationFrame(tick)}requestAnimationFrame(tick);play.onclick=()=>{paused=!paused;stage.classList.toggle('paused',paused);setMotion(paused?'paused':'running');play.textContent=paused?'▶ Play':'Ⅱ Pause';if(!paused){start=performance.now();offset=0}};replay.onclick=()=>{stage.classList.remove('paused');paused=false;setMotion('running');play.textContent='Ⅱ Pause';start=performance.now();offset=0;stage.classList.remove('replay');void stage.offsetWidth;stage.classList.add('replay')};
